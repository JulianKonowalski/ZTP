#include <fstream>
#include <stack>

int main(void) {
	std::ifstream file("test.txt");
	std::stack<float> stack;
	try {
		while (!file.eof()) {

		}
	} catch (std::runtime_error& error) {

	}
	return 0;
}